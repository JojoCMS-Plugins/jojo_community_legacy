This plugin is automatically installed if jojo_community is not installed and a registration page is in the database.

Webmasters should be upgrading to jojo_community, but this plugin is designed to prevent existing sites from breaking.